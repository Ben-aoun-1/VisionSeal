#!/bin/bash
# VisionSeal Automation CLI Wrapper
# Convenience script to run the automation CLI from project root

cd "$(dirname "$0")"
export PYTHONPATH="${PYTHONPATH}:$(pwd)/src"
python3 src/automation/cli.py "$@"