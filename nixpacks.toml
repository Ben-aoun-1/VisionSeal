# Railway Nixpacks configuration for VisionSeal
[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

# Install system dependencies including libmagic
[phases.setup]
nixPkgs = ["python311", "libmagic", "file", "gcc"]

[phases.install]
cmd = "pip install -r requirements.txt"

[start]
cmd = "python main.py"